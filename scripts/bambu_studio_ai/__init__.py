"""Bambu Studio AI: the library behind the skill's command-line scripts.

The scripts in ``scripts/`` are thin entry points; the logic lives here so it can be
typed, tested and imported without side effects.
"""
